import { Text, View } from 'react-native'
import React, { FC } from 'react'


interface TaskProps  {
}

const Task: FC<TaskProps> = (props) => {
    return (
        <View>
        
        </View>
    ) 
}

export default Task